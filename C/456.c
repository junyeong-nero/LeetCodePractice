#include <stdbool.h>
#include <stdio.h>>

bool find132pattern(int* nums, int numsSize) {
    if (numsSize <= 2) return false;
    int i = 0;
    int j = 1;
    while (i < j) {
        }
}

int main() {
    printf("hello world");

    int data[10] = {1, 10, 5, 9, 2, 4, 6, 3, 8, 7};
}